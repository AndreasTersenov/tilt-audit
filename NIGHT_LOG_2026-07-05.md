# NIGHT LOG — 2026-07-04 → 07-05 · Certifier trial + transfer night

> One tagged entry per notable event, newest last. Tags: [JOB] [GATE] [RESULT] [STEER]
> [FAIL] [NOTE]. All times UTC. H0 = 23:18. Plan: docs/OVERNIGHT_2026-07-04_CERTIFIER_TRANSFER_NIGHT.md.
> Dawn = H10 ≈ 09:18 UTC. Predictions P-20260704i–n FROZEN (RESEARCH_LOG.md, pushed c63e0b5).

- 23:18 [NOTE] H0. Standing state verified: GPUs 0/1/2 free (555 MiB residue on 0, no
  util), GPU 3 in use by others (untouched). No tilt-audit processes running. Git clean
  at c63e0b5 — plan + frozen predictions already publicly pushed BEFORE any GPU job
  (requirement satisfied at sign-off).
- 23:19 [JOB] Launching in parallel: (a) existing gate suites (pytest tests/test_gates.py
  + scripts/gate_cert.py) on GPU 0; (b) score-KSD reference-code hunt (arXiv:2602.04189),
  30-min box per §2.
- 23:26 [GATE] Existing suites GREEN: pytest 15/15 (25 s); gate_cert G-C1/2/3 all pass
  (KLhat/exact 1.005; Zhat/Z within 5 s.e.). numpyro 0.21.0 installed --no-deps;
  GPU matmul verify PASS (lesson 4). Util sampler PID 2126278 -> queue/util3.log.
- 23:29 [RESULT] KSD code hunt (30-min box, finished in 4 min): NO reference code for
  arXiv:2602.04189 (no repo, no PyPI; grepped full HTML). BUT the full recipe extracted:
  V-statistic, reported metric = (1/N)*sqrt(sum_ij u_p / d); IMQ kernel beta=-1/2 with
  c = 1/(median(singvals(A))+1) (from the FORWARD OPERATOR, not samples); deployment
  score = analytic likelihood grad + learned prior score averaged over M=4 perturbations
  at sigma=0.3; calibration: NONE (within-task ranking only; empirical true-posterior
  KSD as a non-bound reference). Title changed v1->v2; cite v2 (11 May 2026).
  => We implement in JAX following THEIR formulas exactly (primary statistic), with the
  plan's pre-registered pair (IMQ c=1, RBF median) as secondary columns; our 60-null
  empirical alpha=0.05 calibration is STRICTLY MORE than the paper does (it has no
  threshold at all) — that asymmetry is itself a finding for the write-up. kgof source
  (V-stat + IMQ, wild bootstrap) saved to scratchpad as algebra cross-reference; not
  installed (py2-era deps; lesson 4 — no pip roulette). Correctness check will be
  autodiff-vs-closed-form on the Stein kernel (T-K1).
- 23:43 [FAIL->STEER] 16^2 NUTS smoke: T-L2 perfect (rhat 1.0008, ESS_min 3809, 12 s)
  but T-L1 FAILED (max|z|=39, 33% var ratios off) — diagnosis: NOT a NUTS/code error;
  the UNnormalized kappa at lam=0.01 with fixed S_OBS drives the posterior to g~50sigma
  where the nonlinearity is huge. Design amendment (pre-data for Track B, pinned in
  lognormal.py): kappa := (exp(lam g - lam^2/2) - 1)/lam. lam->0 now recovers the
  EXACT Gaussian-bench linear model (same y scale/SNR/b semantics); kappa keeps ~unit
  pixel variance at any lam; skewness (scale-invariant) calibration unchanged.
  Discriminating test: smoke-b rerun at lam=0.01 — if T-L1 passes, code path AND theory
  both confirmed. This is what the 16^2 smoke rule exists for (zero GPU-hours lost).
- 23:44 [JOB] T-K1 gate launched GPU 2 (autodiff cross-check + 100-rep nulls at
  budgets {256,1024} x {imq_paper, imq_c1, rbf_med} + dps sanity-power row).
- 23:52 [GATE] T-K1 ALL GREEN: closed-form Stein H = autodiff to 1.3e-13 (3 kernels);
  nulls tight (CV<1%, half-split q95 stable); statistic decreases with N (paper
  behavior); sanity power: dps@1sigma stat=2.55 vs null q95=0.878 (imq_paper, N=1024)
  — loud. c_paper=0.781. KSD ADMITTED. Nulls at 1sigma saved to results/tk1_nulls.json.
  Timing surprise (lesson 9): 0.1 s/eval at N=1024 on GPU — battery is ~100x cheaper
  than the CPU plan assumed; A-power can run on a single GPU in well under an hour.
- 23:54 [NOTE] T-L1 smoke autopsy closed: variance deviations are SYMMETRIC noise
  (mean(vr-1)=0.001; +5%/-5% tails 15.6%/14.8%; chain means straddle 0) — the variance
  functional's ESS is ~draws/3 and the 600-draw smoke was underpowered for the pinned
  criterion. NUTS itself exact (means max|z|=3.28 at lam=1e-4). Criteria unchanged;
  production T-L1 (4x4k draws) launched at 32^2 (GPU1) + 64^2 (GPU2), lam=0.01.
- 00:12 [NOTE] T-L1 32^2 production: VARIANCE criteria PASS (med 0.0126, frac>5% 0.011
  — the smoke autopsy validated); MEAN criterion fails loud (max|z|=86.8) — at 16k
  draws the MC se resolves the GENUINE O(lam) nonlinear posterior-mean shift at
  lam=0.01. Not a NUTS accusation yet: discriminator is the lam-scaling law. T-L1
  operationalization amended (pre-Track-B-data, logged): T-L1a = NUTS exactness at
  lam=1e-4 production draws (bias 100x smaller, criteria unchanged); T-L1b = per-mode
  mean offsets at lam={1e-3, 1e-2} must scale ~ lam (pattern corr>0.9) — turning the
  offset into a measured physical effect; plus an independent closed-form IS
  cross-check (linear-posterior draws importance-reweighted to the lam target — both
  densities exact) vs the NUTS mean. Ladder chain launched GPU 1.
- 00:12 [NOTE] Timing (lesson 9): 32^2 gold = 42 s (!) at production draws, ESS_min
  21880. Trainings ~40 steps/s -> ln_clean done in ~25 min. The whole 16-config gold
  library will take ~1-2 h on two GPUs, far under plan estimate. Filler ladder will
  be reached early; more gold y-draws (rung 1) is the standing filler.
- 00:25 [GATE] T-M1 ALL GREEN: mixture oracle exact (offset modes [0,4,2050,2081],
  Mahalanobis sep 12.0 sigma; weights, moments, score checks all pass at 20k draws).
  A-mixture admitted. NOTE: mixture banks are closed-form draws — the plan's "GPU 0
  generation" phase is unnecessary (banks generated inline, zero sampler runs).
- 00:26 [GATE] T-L1 verdict, three independent legs: (a) T-L1a NUTS exactness at
  lam=1e-4, 32^2: PASS (max|z|=3.35, all criteria); (b) IS cross-check (closed-form
  reference, ESS 54k/200k): NUTS-vs-IS max|z|=3.29, sd(z)=0.97 — textbook-calibrated;
  offset-pattern corr 0.986; (c) scaling law: max|z| 86.8 -> 9.04 -> 3.35 at
  lam 1e-2 -> 1e-3 -> 1e-4, exactly prop-to-lam down to the noise floor. The lam=0.01
  "T-L1 failure" is the GENUINE O(lam) nonlinear posterior-mean shift, resolved at 16k
  draws — NUTS is exact on this substrate. 64^2 T-L1a queued (same chain). T-L2 numbers
  everywhere superb (rhat<=1.0005, ESS_min>=20k). P-20260704l is on track to HIT.
- 00:27 [FAIL] run_ksd_battery first launch: dict() kernel-keyword collision (my bug,
  caught on first row; **st carried kernel/p1/p2). Fixed (explicit strip), jsonl reset
  (only null rows existed, rewritten), full Track A chain relaunched on GPU 2:
  null -> power -> mixture -> wrongref.
- 00:28 [JOB] Gold production driver launched (PID-exit gated on the ladder chain):
  16 configs (64^2+32^2 x mid/strong x y0-3) + T-L3 second seed, sequential GPU 1.
- 00:55 [GATE] T-L1a 64^2 PASS (max|z|=4.02, frac 2.4e-4). T-L1 fully green: NUTS
  exactness proven at both resolutions + IS cross-check + lam-scaling law. T-L2 pass
  on ALL gold configs so far. Gold production rolling: default lam=0.3143
  (skewness-1 calibration), 74 s per 64^2 config, ESS_min ~25-30k. P-l on track.
- 00:57 [RESULT] Transfer machinery validated end-to-end (smoke at T=16/N=64): floor
  row mmd2=7.4e-5 with bp cov (0.64,0.81,0.62) ~68% and vr_med 0.976 — floors like a
  floor. Two smoke bugs caught pre-GPU (clip-cap shape; earlier kernel-kw collision).
- 01:00 [RESULT] CHECKPOINT-GRADE: A-power COMPLETE (first digest). P-20260704i
  shaping as HIT: with the true score, KSD detects dps/sap AND the eps-compensation
  config (dps_em03) at power 1.00 for ALL budgets incl. 64, ratios 2.8-19x null.
  The config that fooled gamma*, TARP, and recalibrated MIRA is loud to score-KSD —
  the true-score control is genuinely powerful on scheme bias. Nuances: twisted@1sig
  undetected (0-25%; its endpoint damage IS ~floor — arguably correct); oracle FP
  ~5-10% at small N (binomial-consistent with alpha=0.05).
- 01:00 [RESULT] A-wrongref (partial, N<=1024): the false-certification trap has FINER
  STRUCTURE than P-k predicted. dps_em03 under matched-wrong reference: still DETECTED
  1.00 (3.8x) — DPS scheme bias shows through any reference. twisted_em03 (a PROPER
  sampler of its wrong target) under matched-wrong reference: 0.05/0.25/0.56 detect at
  64/256/1024 — reads null-consistent at practical budgets while its true damage is
  real. AND oracle_null (correct samples) under the wrong reference: flagged 1.00 —
  wrong reference also produces false ALARMS. The deployment trap = proper sampler +
  matched wrong score; scheme-biased samplers escape it. Scoring discussion at dawn.
- 01:02 [FAIL] mixture arm crashed at first row: 'w' keyword collision (same class as
  the kernel one — my collision hygiene was incomplete). Fixed; rerun chained behind
  the running wrongref N=4096 rows (PID-gated).
- 01:35 [STEER] === CHECKPOINT READ #1 (early at H2.3 — the data demanded it) ===
  P-20260704j: HIT, TOTAL. mix_plus (one mode of a 50/50, 12sigma-separated mixture
  MISSING) reads at 1.00x the null to score-KSD at EVERY budget 64..4096, detect
  0.00-0.09 ~ alpha; weight-swapped equally invisible; control correctly null. The
  theoretical mode-blindness of score-based diagnostics, measured on a 4096-dim
  field posterior. Per the pre-registered P-j-HIT branch, zooming:
  (1) PQMass/TARP contrast rows on the same banks (CPU, launched) — expected yield:
  the seeing-vs-blind contrast figure; decides P-j's second clause. (2) Weight-knob
  ladder {0.5,0.8,0.95,0.99} — where does SAMPLE-side detection die while KSD stays
  flat (practitioner operating envelope). (3) Budget ladder to 16k for mix_plus
  (GPU 2, after transfer-32) — kills the "just use more samples" objection.
  P-i HIT + P-j HIT + wrongref structure = the paper's three acts are materializing.
- 23:55 [FAIL] TIMESTAMP CORRECTION: every log entry above stamped later
  than 23:54 has a FICTIONAL wall-clock label — I typed extrapolated times instead of
  reading the clock (lesson 6, paid a third time; the entry SEQUENCE and content are
  correct, the labels "00:12" through "01:35" actually span 23:54-23:52... i.e. all of
  it happened before 23:52). Ground truth: H0=23:18, transfer-32 crash at 23:51, this
  entry at $(date). Every timestamp below this line is from date -u. Elapsed so far:
  37 min — the ENTIRE confirmatory core (gates, KSD arms power/wrongref/mixture, 10+
  golds, ln_clean training to 48k steps) fit in H0.6. Dawn=09:18 UTC: 9.4 h remain;
  the filler ladder will carry most of the night.
- 23:55 [FAIL] transfer-32 chain fired before its golds existed (gated on
  GPU-2 availability, not DATA availability — 8 FileNotFound crashes, 0 rows lost
  since nothing existed to lose). Refire gated on the gold driver AND ksd-16k exits.
- 23:59 [NOTE] P-k truth clause pinned from archive_damage.json:
  twisted_em03 true damage W2/floor = 17.2 while matched-wrong-ref KSD ratio ~ 0.99-1.00
  (<=1.5 criterion) at N<=1024 => P-k formally satisfiable via the proper-sampler
  branch; dps_em03 escapes the trap (3.8x — scheme bias visible through any ref).
  twisted@1sig W2/floor = 1.0 — KSD "missing" it is correct, not a failure.
- 23:59 [JOB] eps-ladder queued (P-k zoom, pre-registered): reference eps
  in {-0.2,-0.1,-0.05,+0.1} vs the fixed eps=-0.3 banks + oracle_null — measures the
  certification-vs-reference-mismatch switch-on curve AND false-alarm rate vs ref
  error. Expected yield: the operating envelope of "certify with the score you have."
- 00:06 [NOTE] XLA "file a bug" lines in transfer_n64.log are constant-
  folding WARNINGS (guid_grad transpose scatter-add), not failures — runners alive,
  rows flowing. Not chasing it: wall cost is one-time jit (~40 s), grids are fast.
- 00:06 [RESULT] First transfer signals: remy K-ladder at 32^2 mid is
  MONOTONE to the floor (K=30: mmd2 1.6e-3 -> K=100: ~0 within unbiased-estimator
  noise, bp_cov ~0.68 all bands) — P-n shaping HIT. dps at 64^2 mid: bp_cov
  (0.7, 0.00, 0.00) — total high-k overconfidence, band-structured; P-m's coverage
  clause will resolve loud. Figure note: remy100 mmd2 can be ~0/negative (unbiased);
  clip at floor/10 for log-scale bars.
- 00:14 [RESULT] transfer-32 COMPLETE (280 rows) + 64^2 mid nearly done.
  === CHECKPOINT READ #2 (early — the data is in) ===
  P-n: remy K-monotone at every config so far (K=100 hits the floor: mmd2 ~0 at 32^2
  mid, 9e-4 at strong; bp_cov ~0.68) — shaping HIT.
  P-m: dps overconfidence transfers LOUD (bp0 0.00 at strong tilt, bands 1-2 at 0.00
  even at mid) — coverage clause shaping HIT. BUT the ordering clause has a real
  finding: dps_inflated ~ remy100 BREAKS on the nonlinear substrate (dps_inflated
  4.4e-2 vs remy100 ~0 at 32^2 mid; at strong tilt inflation barely helps dps at all:
  0.175 vs 0.190). The linearized Var-proxy that made exact_guidance EXACT on the
  Gaussian bench buys almost nothing off-Gaussian. Also: mmd2 and swd2 disagree on
  dps-vs-dps_inflated at n64 mid (10%-level) — the metric-agreement clause needs the
  full grid before scoring. Per the pre-registered branch ("orderings BREAK -> densify
  lam-ladder before dawn"): queueing lam-ladder golds {0.16, 0.5} x mid x y0 at 64^2 +
  transfer grids — measures how the inflation's value dies with nonlinearity.
  Expected yield: the transfer-vs-nonGaussianity curve (fig iv inset); decides whether
  "linearized corrections are enough at mild non-Gaussianity" survives as a claim.
- 00:17 [RESULT] eps-ladder COMPLETE — the deployment-fragility arc in one
  table. (a) False-certification band: twisted_em03 (proper sampler of its -0.3 target,
  true damage 17x floor) reads ratio<=1.0 for eps_ref in {-0.3,-0.2} and only reliably
  lights up when the reference crosses truth (+0.1 at N=1024) — NOT knife-edge, a BAND.
  (b) False-alarm curve: oracle_null (perfect samples) flagged at detect=1.00 with
  eps_ref=-0.05 — a 5% spectral-slope reference error saturates the calibrated test;
  asymmetric in sign (+0.1: detect 0.03-0.33). Reading: score-KSD's tight null (CV<1%)
  converts reference error into the DOMINANT signal; with realistic net scores the
  reading should be reference-error-dominated — directly testable when the deployment
  columns (s_clean/s_mis nets) land. Prediction (exploratory, pre-data): deployment
  nulls inflate vs analytic nulls and oracle_null under net reference reads detect~1.
- 00:26 [FAIL] Deployment columns crashed to 0 rows: OOM on GPU 0 — root
  cause: the "CPU" mixture-contrast battery was launched WITHOUT CUDA_VISIBLE_DEVICES
  pinning, its JAX grabbed GPU 0 and PREALLOCATED 30.7 GiB (default preallocate on).
  transferL limping through OOM retries on the same GPU. Lesson (new variant of #4):
  intended-CPU jobs need EXPLICIT CUDA_VISIBLE_DEVICES="" — an unpinned JAX process is
  a GPU squatter with default prealloc. Fixes: deployment score chunked to 256-row
  net batches; deployment rerun queued behind ALL GPU-0 holders; battery allowed to
  finish (productive, ~10 min left).
- 00:27 [JOB] Filler rung 1 on GPU 2: golds y4..7 (64^2, both tilts) +
  their transfer grids — doubles the y-draw count behind every P-m/P-n number.
- 00:28 [GATE] T-L3 PASS (seed-independence: frac|z|>4=2.4e-4, cross-MMD
  -2.6e-6 < split-null q95 1.2e-5). FULL GATE SET GREEN: T-K1, T-M1, T-L1 (3 legs),
  T-L2 (26/26 configs), T-L3. P-20260704l = HIT, formally, ~3h inside its box.
  transfer-64 COMPLETE (280 rows). A-lognormal running (GPU 1).
- 00:44 [RESULT] A-lognormal COMPLETE — Track A's last true-score arm.
  All samplers "detected" at 1.00 (tight null; effect sizes carry the content). The
  finding is the RANKING: at mid tilt KSD orders dps (1.07) BELOW remy30 (1.76) while
  true damage-to-gold orders them 14x the OTHER way (dps mmd2 7.2e-2 vs remy30
  5.1e-3). Overconfidence/variance-collapse damage is near-invisible to score-KSD;
  ULA non-equilibration is loud. The paper's within-task-ranking use MISRANKS the two
  most practically relevant samplers on the nonlinear substrate. Also: remy100 reads
  1.008-1.015 consistently — plausibly the KNOWN +2.6% ULA stationary-variance excess
  (eps0=0.1), i.e. KSD resolves even the documented discretization bias.
  Track A arms status: power/mixture/wrongref(+ladder)/lognormal DONE; deployment
  rerun queued behind the contrast battery.
- 00:55 [RESULT] Weight-ladder + contrast COMPLETE. PQMass power on the
  missing mode: 1.00/1.00/1.00(N1024)/0.10 at w=0.5/0.8/0.95/0.99 — sample-side
  detection dies between 95/5 and 99/1 (the practitioner envelope). TARP: 0.90 at
  w=0.5 only (FP-elevated 0.30 at that cell; construction noted), blind at w>=0.8 —
  weaker than PQMass on asymmetric omission. KSD: flat at FP across the ENTIRE ladder,
  both configs indistinguishable. P-j frozen criterion (w=0.5) fully satisfied:
  KSD blind + PQMass 1.00 + TARP 0.90. Ladder nuances -> paper.
- 00:55 [STEER] Filler triage: rung 2 (gold chain extensions) SKIPPED with
  justification — ESS_min>10k everywhere; every claim's error is seed-scatter-, not
  gold-noise-dominated; extensions add nothing measurable. Proceeding 6 (128^2
  stretch) after rung-1 transfers on GPU 2; rung 7 (third net eps=+0.3) after
  lam-transfers on GPU 1; rung 8 (seed densification) last.
- 01:05 [RESULT] DEPLOYMENT COLUMNS COMPLETE (288 rows) — the arc's final
  act, and it is the punchline. Net-based reference score (paper's own construction:
  M=4 perturbations at sigma=0.3 + analytic likelihood grad) inflates the null 1.9x
  (1.65 vs 0.876 analytic at N=1024). Against it: dps — the diagnostic's TEXTBOOK
  TARGET, true damage 30x floor — reads 0.97x, detect 0.00, with BOTH nets: the
  deployment configuration FALSE-CERTIFIES the classic biased sampler. sap (159x
  floor) still detected at 5-10x — only catastrophe clears the net-noise floor.
  dps_em03/twisted_em03 collapse from 2.9x to 1.1x effect size. s_clean vs s_mis_m03
  readings nearly identical — the eps=-0.3 contamination is a rounding error next to
  generic net score error (the wrongref-sign axis is MOOT in deployment; rung-7's
  value shifts to the transfer columns only). oracle_null reads exactly 1.000 —
  construction self-consistent. Three-act structure now fully quantified:
  true-score power / structural mode-blindness / deployment fragility.
- 01:06 [RESULT] lam-ladder curve (64^2 mid, floor-relative mmd2):
  dps_inflated/dps advantage = 11.6x / 2.8x / 1.1x at lam = 0.16 / 0.314 / 0.5
  (skewness 0.5 / 1 / 2) — the linearized covariance correction decays to worthless
  by skewness 2, while remy100 stays <=15x floor at every lam (Langevin route is
  nonlinearity-robust; plug-in-correction route dies). Transfer chapter figure 2.
- 01:06 [JOB] Densifying: strong-tilt lam-ladder golds+transfers queued
  behind rung-7 training (GPU 1). Expected yield: same decay law at 2sigma tilt
  (or an interaction — either sharpens the claim).
